This file is a merged representation of a subset of the codebase, containing specifically included files and files not matching ignore patterns, combined into a single document by Repomix.
The content has been processed where comments have been removed, empty lines have been removed, content has been compressed (code blocks are separated by ⋮---- delimiter).

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of a subset of the repository's contents that is considered the most important context.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.

- Pay special attention to the Repository Instruction. These contain important context and guidelines specific to this project.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Only files matching these patterns are included: src/**/*, package.json, README.md
- Files matching these patterns are excluded: **/*.test.ts, **/*.spec.ts, docs/**, *.log, src/app/globals.css, src/components/ui/**, src/lib/prompts/**
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Code comments have been removed from supported file types
- Empty lines have been removed from all files
- Content has been compressed - code blocks are separated by ⋮---- delimiter
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
src/
  app/
    (app)/
      advisor/
        page.tsx
      card-index/
        page.tsx
      dashboard/
        page.tsx
      library/
        actions.ts
        library-content.tsx
        page.tsx
      actions.ts
      layout.tsx
    (auth)/
      login/
        actions.ts
        page.tsx
      onboarding/
        _components/
          onboarding-content.tsx
          onboarding-stepper.tsx
          start-over-button.tsx
        _lib/
          fetch-actions.ts
        boxes/
          _components/
            boxes-container.tsx
          actions.ts
          page.tsx
        enrichment/
          _components/
            enrichment-view.tsx
          actions.ts
          page.tsx
        literature-review/
          _components/
            literature-article-card.tsx
            literature-review-content.tsx
          _hooks/
            use-literature-review.ts
          _services/
            ai-processor.ts
            literature-review-papers.ts
            search-api.ts
          actions.ts
          page.tsx
        matrix/
          _components/
            matrix-form.tsx
          actions.ts
          page.tsx
        risk/
          _components/
            originality-report-view.tsx
            risk-container.tsx
            strategic-roadmap-section.tsx
            tavily-fact-check-table.tsx
            tezara-overlap-table.tsx
          _hooks/
            use-risk-analysis.ts
          _lib/
            constants.ts
          _services/
            analysis.ts
            queries.ts
            roadmap.ts
            search.ts
            sifting.ts
          actions.ts
          error.tsx
          loading.tsx
          page.tsx
        actions.ts
        layout.tsx
        page.tsx
      layout.tsx
    api/
      route.ts
    layout.tsx
    page.tsx
  components/
    providers/
      query-provider.tsx
    error-display.tsx
    header.tsx
    literature-reader.tsx
    onboarding-global-loader.tsx
  db/
    index.ts
    schema.ts
    seed.ts
  lib/
    store/
      onboarding-store.ts
    cloudflare.ts
    error-utils.ts
    gemini.ts
    logger.ts
    tavily.ts
    tezara-parser.ts
    tezara.ts
    types.ts
    utils.ts
  proxy.ts
package.json
README.md
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="src/app/(app)/advisor/page.tsx">
export default function AdvisorPage()
</file>

<file path="src/app/(app)/card-index/page.tsx">
export default function CardIndexPage()
</file>

<file path="src/app/(app)/dashboard/page.tsx">
export default function DashboardPage()
</file>

<file path="src/app/(auth)/onboarding/_lib/fetch-actions.ts">
import { eq } from "drizzle-orm";
import { db } from "@/db";
import { thesisMatrices, originalityReports, thesisBoxes } from "@/db/schema";
import { getSession } from "@/proxy";
⋮----
export async function fetchThesisMatrix()
⋮----
export async function fetchOriginalityReport()
⋮----
export async function fetchBoxes()
</file>

<file path="src/app/(auth)/onboarding/literature-review/_services/literature-review-papers.ts">
import type { FoundationalQuery } from "@/lib/types";
⋮----
export interface SubBoxInput {
  title: string;
  description: string;
  semanticSearchBlock: string;
  foundationalQueries: FoundationalQuery[];
}
⋮----
export interface RawPaper {
  source: "openalex";
  title: string | null;
  abstract: string | null;
  metadata: string | null;
  doi: string | null;
  url: string | null;
  authors: string[];
  year: number | null;
  publisher: string | null;
  openAlexId: string | null;
  isFoundational: boolean;
  relevanceScore: number;
}
⋮----
export interface ValidatedPaper {
  title: string;
  abstract: string | null;
  metadata: string | null;
  doi: string | null;
  url: string | null;
  authors: string[];
  year: number | null;
  publisher: string | null;
  openAlexId: string | null;
  isFoundational: boolean;
  relevanceScore: number;
}
⋮----
export function mergePapers(
  papers: RawPaper[],
): ValidatedPaper[]
⋮----
function ingest(raw: RawPaper): void
</file>

<file path="src/app/api/route.ts">
import { NextResponse } from "next/server";
⋮----
export function GET()
</file>

<file path="src/app/page.tsx">
import { redirect } from "next/navigation";
⋮----
export default function RootPage()
</file>

<file path="src/components/providers/query-provider.tsx">
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { useState } from "react";
⋮----
export default function QueryProvider({
  children,
}: {
  children: React.ReactNode;
})
</file>

<file path="src/db/seed.ts">
import { hash } from "bcrypt-ts";
import { db } from "./index";
import { users } from "./schema";
⋮----
async function seed()
</file>

<file path="README.md">
# fabricca
</file>

<file path="src/app/(app)/library/library-content.tsx">
import { useState } from "react";
import { Layers } from "lucide-react";
import { LiteratureReader } from "@/components/literature-reader";
import { ScrollArea } from "@/components/ui/scroll-area";
import type { ThesisBox } from "@/db/schema";
⋮----
interface LibraryContentProps {
  boxes: ThesisBox[];
}
</file>

<file path="src/app/(auth)/onboarding/_components/onboarding-content.tsx">
import { useOnboardingStore } from "@/lib/store/onboarding-store";
import { OnboardingGlobalLoader } from "@/components/onboarding-global-loader";
⋮----
export function OnboardingContent({
  children,
}: Readonly<{
  children: React.ReactNode;
}>)
</file>

<file path="src/app/(auth)/onboarding/risk/_components/tavily-fact-check-table.tsx">
import { ShieldCheck, FileText, ExternalLink } from "lucide-react";
import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
} from "@/components/ui/card";
import type { OriginalityReportData } from "@/lib/types";
⋮----
interface TavilyFactCheckTableProps {
  tavilyResults: OriginalityReportData["tavilyResults"];
}
</file>

<file path="src/app/(auth)/onboarding/risk/_components/tezara-overlap-table.tsx">
import { useMemo, useState, Fragment } from "react";
import { GitCompare, ChevronDown, ChevronRight } from "lucide-react";
import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
} from "@/components/ui/card";
import { getThesisPriority } from "../_services/analysis";
import { statusTranslation, getBadgeColor } from "../_lib/constants";
import type { OriginalityReportData } from "@/lib/types";
⋮----
type OverlapRow =
  OriginalityReportData["tezaraResults"]["overlapTable"][number];
⋮----
interface TezaraOverlapTableProps {
  overlapTable: OriginalityReportData["tezaraResults"]["overlapTable"];
  strategicRecommendations: string;
}
⋮----
setExpandedThesisId(
</file>

<file path="src/app/(auth)/onboarding/risk/_lib/constants.ts">
export const getBadgeColor = (key: string): string
</file>

<file path="src/components/header.tsx">
import { startTransition } from "react";
import Link from "next/link";
import Image from "next/image";
import { usePathname } from "next/navigation";
import {
  LayoutDashboard,
  BookOpen,
  Layers,
  MessageSquareCode,
  LogOut,
} from "lucide-react";
import { Button } from "@/components/ui/button";
import { logoutAction } from "@/app/(app)/actions";
import { cn } from "@/lib/utils";
⋮----
export function Header(
⋮----
function handleLogout()
⋮----
className=
</file>

<file path="src/components/literature-reader.tsx">
import { useRef, useEffect, useCallback } from "react";
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { CheckCircle, BookOpen, RefreshCw, Layers } from "lucide-react";
import { toast } from "sonner";
import { Checkbox } from "@/components/ui/checkbox";
import { Badge } from "@/components/ui/badge";
import { ScrollArea } from "@/components/ui/scroll-area";
import { Card, CardContent, CardHeader } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import {
  getApprovedResourcesAction,
  toggleResourceReadStatusAction,
  replenishFromReservedAction,
} from "@/app/(app)/library/actions";
import type {
  GetApprovedResourcesResult,
  ToggleReadStatusResult,
  ReplenishResult,
} from "@/app/(app)/library/actions";
⋮----
interface LiteratureReaderProps {
  boxId: number;
  boxTitle: string;
}
⋮----
type ReaderStatus =
  | "loading"
  | "error"
  | "reading"
  | "all-read"
  | "replenishing";
⋮----
const APPROVED_KEY = (boxId: number)
⋮----
export function LiteratureReader(
⋮----
onClick=
</file>

<file path="src/lib/tezara-parser.ts">
import type { TezaraThesisSummary } from "./types";
⋮----
export interface TezaraRawThesisObj {
  id?: number | string;
  title_original?: string;
  title_translated?: string;
  author?: string;
  university?: string;
  year?: number | string;
  thesis_type?: string;
  thesisType?: string;
  department?: string;
  abstract_original?: string;
  abstract_translated?: string;
}
⋮----
export function extractJsonObjects(text: string): unknown[]
⋮----
/**
 * Extracts RSC stream text fragments mapped by their reference key.
 *
 * @param text - The raw RSC response text.
 * @returns A mapping of reference IDs to their resolved text contents.
 */
export function extractRscTexts(text: string): Record<string, string>
⋮----
// Ensure the key looks like a valid alphanumeric reference identifier
⋮----
// If the content starts with a type prefix (like T12, etc.) followed by a comma
// e.g. "T12,Özet" or "T12,\"Özet\""
⋮----
// If it is wrapped in quotes, try to parse it as a JSON string to handle unescaping
⋮----
// Fallback: save content as is, trimmed
⋮----
/**
 * Maps a raw thesis object from a Tezara RSC response to a typed TezaraThesisSummary.
 * Returns null if the raw object has no valid identifier.
 *
 * @param raw - The raw object from the Tezara response
 * @returns A typed summary object, or null
 */
function mapToThesisSummary(raw: Record<string, unknown>): TezaraThesisSummary | null
⋮----
/**
 * Recursively searches any value to find and extract objects that match the thesis summary structure.
 *
 * @param val - The value to scan.
 * @param results - The accumulator array of thesis summaries.
 */
function findThesesRecursively(val: unknown, results: TezaraThesisSummary[]): void
⋮----
// If the object looks like a thesis summary (must have title and some identifier or author)
⋮----
// Recurse into all properties
⋮----
/**
 * Parses the thesis search results from raw RSC stream text response.
 *
 * @param text - The raw RSC response text.
 * @returns A list of parsed thesis summary objects.
 */
export function parseRscTheses(text: string): TezaraThesisSummary[]
⋮----
// Navigate the standard Next.js query cache state structure safely
⋮----
// Fallback: If no results were found via the structured path, search recursively
⋮----
/**
 * Recursively searches a JSON structure to find the thesis object details.
 *
 * @param val - The value to scan.
 * @returns The found raw thesis object or null.
 */
export function findThesisObjRecursively(val: unknown): TezaraRawThesisObj | null
⋮----
// Check if the object contains a nested 'thesis' object with academic fields
</file>

<file path="src/lib/utils.ts">
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";
⋮----
export function cn(...inputs: ClassValue[])
⋮----
export function cosineSimilarity(vecA: number[], vecB: number[]): number
</file>

<file path="src/app/(app)/library/page.tsx">
import { eq, asc } from "drizzle-orm";
import { redirect } from "next/navigation";
import { db } from "@/db";
import { thesisMatrices, thesisBoxes } from "@/db/schema";
import { getSession } from "@/proxy";
import { LibraryContent } from "./library-content";
⋮----
export default async function LibraryPage()
</file>

<file path="src/app/(app)/actions.ts">
import { cookies } from "next/headers";
import { redirect } from "next/navigation";
import { createFlowId, Logger } from "@/lib/logger";
⋮----
export async function logoutAction()
</file>

<file path="src/app/(auth)/onboarding/boxes/page.tsx">
import { redirect } from "next/navigation";
import { getProfile } from "@/proxy";
import { BoxesContainer } from "./_components/boxes-container";
⋮----
export default async function OnboardingBoxesPage()
</file>

<file path="src/app/(auth)/onboarding/literature-review/page.tsx">
import { redirect } from "next/navigation";
import { getProfile } from "@/proxy";
import { eq } from "drizzle-orm";
import { db } from "@/db";
import { thesisMatrices } from "@/db/schema";
import { LiteratureReviewContent } from "./_components/literature-review-content";
⋮----
export default async function LiteratureReviewPage()
</file>

<file path="src/app/(auth)/onboarding/risk/_components/strategic-roadmap-section.tsx">
import { Compass } from "lucide-react";
⋮----
interface StrategicRoadmapSectionProps {
  strategicRecommendations: string;
}
⋮----
export function StrategicRoadmapSection({
  strategicRecommendations,
}: StrategicRoadmapSectionProps)
</file>

<file path="src/app/(auth)/onboarding/risk/loading.tsx">
import { Loader2 } from "lucide-react";
⋮----
export default function RiskLoading()
</file>

<file path="src/app/layout.tsx">
import type { Metadata } from "next";
import { Poppins, Fredoka, JetBrains_Mono } from "next/font/google";
⋮----
import QueryProvider from "@/components/providers/query-provider";
import { Toaster } from "@/components/ui/sonner";
⋮----
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>)
</file>

<file path="src/db/index.ts">
import { Pool } from "@neondatabase/serverless";
import { drizzle } from "drizzle-orm/neon-serverless";
</file>

<file path="src/lib/cloudflare.ts">
import type { Logger } from "./logger";
⋮----
interface CloudflareEmbeddingItem {
  object: string;
  embedding: number[];
  index: number;
}
⋮----
interface CloudflareEmbeddingResponse {
  data: CloudflareEmbeddingItem[];
  model: string;
}
⋮----
export async function generateEmbeddings(
  texts: string[],
  logger?: Logger,
): Promise<number[][]>
</file>

<file path="src/lib/error-utils.ts">
export type ErrorScenario = "quota" | "network" | "system";
⋮----
export interface ErrorDisplay {
  title: string;
  description: string;
  scenario: ErrorScenario;
  canRetry: boolean;
}
⋮----
export function classifyError(error: unknown): ErrorScenario
⋮----
export function getErrorDisplay(error: unknown): ErrorDisplay
⋮----
export function extractMessage(error: unknown): string
</file>

<file path="src/app/(app)/library/actions.ts">
import { and, eq, inArray, asc, sql } from "drizzle-orm";
import { redirect } from "next/navigation";
import { db } from "@/db";
import { libraryResources, type LibraryResource } from "@/db/schema";
import { getSession } from "@/proxy";
import { createFlowId, Logger } from "@/lib/logger";
⋮----
export type GetApprovedResourcesResult =
  | { success: true; data: LibraryResource[] }
  | { success: false; error: string };
⋮----
export type ToggleReadStatusResult =
  | { success: true }
  | { success: false; error: string };
⋮----
export type ReplenishResult =
  | { success: true; data: LibraryResource[] }
  | { success: false; error: string };
⋮----
export async function getApprovedResourcesAction(
  boxId: number
): Promise<GetApprovedResourcesResult>
⋮----
export async function toggleResourceReadStatusAction(
  resourceId: number,
  isRead: boolean
): Promise<ToggleReadStatusResult>
⋮----
export async function replenishFromReservedAction(
  boxId: number
): Promise<ReplenishResult>
</file>

<file path="src/app/(auth)/onboarding/enrichment/page.tsx">
import { redirect } from "next/navigation";
import { getProfile } from "@/proxy";
import { EnrichmentView } from "./_components/enrichment-view";
import { StartOverButton } from "../_components/start-over-button";
⋮----
export default async function OnboardingEnrichmentPage()
</file>

<file path="src/app/(auth)/onboarding/literature-review/_hooks/use-literature-review.ts">
import { useMemo, useEffect, useState, useCallback, useRef } from "react";
import { useRouter } from "next/navigation";
import { toast } from "sonner";
import { useOnboardingStore } from "@/lib/store/onboarding-store";
import type { LoadingStep } from "@/lib/store/onboarding-store";
import type { GeminiThesisBox, LiteraturePoolEntry } from "@/lib/types";
import {
  processLiteratureReviewAction,
  confirmLiteratureAction,
} from "../actions";
import { fetchBoxes } from "../../_lib/fetch-actions";
⋮----
export type BoxStatus = "idle" | "loading" | "done" | "error";
⋮----
export interface UseLiteratureReviewResult {
  subBoxes: GeminiThesisBox[];
  loading: boolean;
  processing: boolean;
  confirming: boolean;
  boxStatuses: Record<string, BoxStatus>;
  boxErrors: Record<string, string>;
  allProcessed: boolean;
  literaturePool: LiteraturePoolEntry[];
  startReviewProcess: () => Promise<void>;
  handleFinalize: () => Promise<void>;
}
⋮----
export function useLiteratureReview(): UseLiteratureReviewResult
⋮----
// router intentionally excluded — redirect on mount only.
// eslint-disable-next-line react-hooks/exhaustive-deps
⋮----
/**
   * Runs the literature-review pipeline over all sub-boxes in fixed-size chunks
   * (see {@link CHUNK_SIZE}). Each chunk is processed in parallel via the bulk
   * server action; results are streamed into the Zustand pool and the global
   * loading overlay is advanced step-by-step.
   */
</file>

<file path="src/app/(auth)/onboarding/matrix/page.tsx">
import { redirect } from "next/navigation";
import { getProfile } from "@/proxy";
import { MatrixForm } from "./_components/matrix-form";
import { StartOverButton } from "../_components/start-over-button";
⋮----
export default async function OnboardingMatrixPage()
</file>

<file path="src/app/(auth)/onboarding/risk/error.tsx">
import { ErrorDisplay } from "@/components/error-display";
⋮----
export default function OnboardingRiskError({
  error,
  reset,
}: {
  error: Error & { digest?: string };
reset: ()
</file>

<file path="src/components/error-display.tsx">
import { useEffect } from "react";
import { AlertTriangle, WifiOff, Clock, RefreshCw } from "lucide-react";
import { Button } from "@/components/ui/button";
import {
  getErrorDisplay,
  type ErrorDisplay as ErrorDisplayType,
} from "@/lib/error-utils";
⋮----
interface ErrorDisplayProps {
  error: unknown;
  onRetry?: () => void;
}
</file>

<file path="src/components/onboarding-global-loader.tsx">
import { useOnboardingStore } from "@/lib/store/onboarding-store";
</file>

<file path="src/lib/store/onboarding-store.ts">
import { create } from "zustand";
import { persist, createJSONStorage } from "zustand/middleware";
import type {
  GeminiThesisBox,
  LiteraturePoolEntry,
  OriginalityReportData,
} from "@/lib/types";
⋮----
interface PersistedOnboardingState {
  boxes: GeminiThesisBox[] | null;
  literaturePool: LiteraturePoolEntry[];
  reportData: OriginalityReportData | null;
}
⋮----
export interface LoadingStep {
  text: string;
  status: "idle" | "active" | "completed";
}
⋮----
interface LoadingActions {
  showLoading: (
    title: string,
    description: string,
    steps: LoadingStep[],
  ) => void;
  updateLoadingStep: (
    index: number,
    status: "idle" | "active" | "completed",
  ) => void;
  hideLoading: () => void;
}
⋮----
interface LoadingState {
  isLoading: boolean;
  loadingTitle: string;
  loadingDescription: string;
  loadingSteps: LoadingStep[];
}
⋮----
interface OnboardingState extends LoadingState {
  boxes: GeminiThesisBox[] | null;
  setBoxes: (boxes: GeminiThesisBox[] | null) => void;
  reportData: OriginalityReportData | null;
  setReportData: (data: OriginalityReportData | null) => void;
  clearReportData: () => void;
  literaturePool: LiteraturePoolEntry[];
  setLiteraturePool: (pool: LiteraturePoolEntry[]) => void;
  addToLiteraturePool: (entry: LiteraturePoolEntry) => void;
  resetStore: () => void;
}
⋮----
type OnboardingStore = OnboardingState & LoadingActions;
⋮----
// ── Persisted state ──
</file>

<file path="src/lib/tavily.ts">
import type { Logger } from "./logger";
⋮----
export interface TavilySearchResult {
  title: string;
  url: string;
  content: string;
  score: number;
  rawContent?: string;
}
⋮----
export interface TavilySearchResponse {
  query: string;
  responseTime: number;
  results: TavilySearchResult[];
}
⋮----
export async function tavilySearch(
  query: string,
  logger?: Logger,
): Promise<TavilySearchResponse>
</file>

<file path="src/app/(app)/layout.tsx">
import { redirect } from "next/navigation";
import { getSessionWithOnboarding } from "@/proxy";
import { Header } from "@/components/header";
⋮----
export default async function AppLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>)
</file>

<file path="src/app/(auth)/login/actions.ts">
import { cookies } from "next/headers";
import { eq } from "drizzle-orm";
import { compare } from "bcrypt-ts";
import { db } from "@/db";
import { users } from "@/db/schema";
import { getSession } from "@/proxy";
import { createFlowId, Logger } from "@/lib/logger";
⋮----
export type LoginResult =
  | { success: true; error?: never }
  | { success?: never; error: string };
⋮----
export async function loginAction(
  email: string,
  password: string,
): Promise<LoginResult>
⋮----
export type OnboardingStatusResult =
  | {
      onboardingCompleted: boolean;
      error?: never;
    }
  | { onboardingCompleted?: never; error: string };
⋮----
export async function checkOnboardingStatus(): Promise<OnboardingStatusResult>
</file>

<file path="src/app/(auth)/onboarding/risk/page.tsx">
import { redirect } from "next/navigation";
import { getProfile } from "@/proxy";
import { RiskContainer } from "./_components/risk-container";
⋮----
export default async function OnboardingRiskPage()
</file>

<file path="src/app/(auth)/onboarding/layout.tsx">
import { OnboardingStepper } from "./_components/onboarding-stepper";
import { OnboardingContent } from "./_components/onboarding-content";
⋮----
export default function OnboardingLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>)
</file>

<file path="src/app/(auth)/login/page.tsx">
import { useState, startTransition } from "react";
import { useRouter } from "next/navigation";
import Image from "next/image";
import { toast } from "sonner";
⋮----
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { loginAction, checkOnboardingStatus } from "./actions";
⋮----
export default function LoginPage()
⋮----
/**
   * Form gönderimini yönetir.
   * Server action'ı startTransition ile güvenli şekilde tetikler,
   * sonucu toast bildirimi olarak gösterir.
   * Başarılı giriş sonrası onboarding durumunu sorgulayarak
   * uygun sayfaya yönlendirme yapar.
   *
   * @param e - Form submit olayı
   */
function handleSubmit(e: React.FormEvent<HTMLFormElement>)
⋮----
onChange=
</file>

<file path="src/app/(auth)/onboarding/_components/onboarding-stepper.tsx">
import { usePathname, useRouter } from "next/navigation";
import { Check } from "lucide-react";
import { cn } from "@/lib/utils";
⋮----
className=
</file>

<file path="src/app/(auth)/onboarding/literature-review/_components/literature-article-card.tsx">
import {
  Card,
  CardContent,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";
import type { JuryArticle } from "@/lib/types";
⋮----
interface LiteratureArticleCardProps {
  article: JuryArticle;
}
</file>

<file path="src/app/(auth)/onboarding/risk/_hooks/use-risk-analysis.ts">
import { useEffect, useState, useCallback } from "react";
import { useRouter } from "next/navigation";
import { toast } from "sonner";
import { useOnboardingStore } from "@/lib/store/onboarding-store";
import type { LoadingStep } from "@/lib/store/onboarding-store";
import type { OriginalityReportData } from "@/lib/types";
import type { ThesisMatrix } from "@/db/schema";
import {
  extractQueriesAction,
  executeSearchAction,
  siftThesesAction,
  finalizeJuryAnalysisAction,
  completeRiskStageAction,
} from "../actions";
import { generateBoxesAction } from "../../boxes/actions";
import {
  fetchThesisMatrix,
  fetchOriginalityReport,
} from "../../_lib/fetch-actions";
⋮----
export interface UseRiskAnalysisResult {
  loading: boolean;
  analysing: boolean;
  error: string | null;
  reportData: OriginalityReportData | null;
  proceeding: boolean;
  startAnalysis: () => Promise<void>;
  handleProceed: () => Promise<void>;
}
⋮----
export function useRiskAnalysis(): UseRiskAnalysisResult
⋮----
async function init()
</file>

<file path="src/app/(auth)/layout.tsx">
import { redirect } from "next/navigation";
import { getSessionWithOnboarding } from "@/proxy";
⋮----
export default async function AuthLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>)
</file>

<file path="src/proxy.ts">
import { cookies } from "next/headers";
import { redirect } from "next/navigation";
import { eq } from "drizzle-orm";
import { db } from "@/db";
import { users } from "@/db/schema";
⋮----
export type SessionUser = {
  userId: number;
  name: string;
  onboardingCompleted?: boolean;
};
⋮----
export type SessionWithOnboarding = SessionUser & {
  onboardingCompleted: boolean;
};
⋮----
export async function getSession(): Promise<SessionUser | null>
⋮----
export async function getSessionWithOnboarding(): Promise<SessionWithOnboarding | null>
⋮----
export async function getProfile()
</file>

<file path="src/app/(auth)/onboarding/enrichment/actions.ts">
import { eq } from "drizzle-orm";
import { db } from "@/db";
import { thesisMatrices, originalityReports, thesisBoxes } from "@/db/schema";
import { getSession } from "@/proxy";
import { createFlowId, Logger } from "@/lib/logger";
import type { EnhancedThesisData, OnboardingActionResult } from "@/lib/types";
⋮----
export async function confirmEnhancedThesisAction(
  data: EnhancedThesisData,
): Promise<OnboardingActionResult>
</file>

<file path="src/app/(auth)/onboarding/risk/_services/roadmap.ts">
import { ThinkingLevel } from "@google/genai";
import { generateStructuredContent } from "@/lib/gemini";
import { extractMessage } from "@/lib/error-utils";
import type { Logger } from "@/lib/logger";
import {
  roadmapSchema,
  buildRoadmapSystemInstruction,
  buildRoadmapPrompt,
} from "@/lib/prompts";
⋮----
export interface SynthesizeRoadmapParams {
  studyTitle: string;
  researchQuestion: string;
  mainClaim: string;
  theoreticalFramework: string;
  methodology: string;
  dataStrategy: string;
  historicalLimits: string;
  spatialLimits: string;
  analyticalFocus: string;
  comparisonResults: {
    title: string;
    author: string;
    year: number;
    axes: {
      subject: string;
      theory: string;
      methodology: string;
      context?: string;
    };
    originalityLevel: "HIGH_RISK" | "MEDIUM_RISK" | "LOW_RISK" | "ZERO_RISK";
    comparisonNote: string;
  }[];
}
⋮----
export async function synthesizeRoadmap(
  params: SynthesizeRoadmapParams,
  log: Logger,
): Promise<string>
</file>

<file path="src/app/(auth)/onboarding/risk/_services/search.ts">
import { ThinkingLevel } from "@google/genai";
import { tavilySearch } from "@/lib/tavily";
import { searchTezara } from "@/lib/tezara";
import { generateStructuredContent } from "@/lib/gemini";
import { extractMessage } from "@/lib/error-utils";
import type { Logger } from "@/lib/logger";
import type {
  TezaraThesisSummary,
  TavilyEvaluationResponse,
} from "@/lib/types";
import {
  tavilyEvaluationSchema,
  buildTavilyEvalSystemInstruction,
  buildTavilyEvalPrompt,
} from "@/lib/prompts";
⋮----
export async function executeParallelSearch(
  tavilyQueries: string[],
  tezaraQueries: string[],
  log: Logger,
): Promise<
⋮----
export interface EvaluateTavilyParams {
  studyTitle: string;
}
⋮----
export async function evaluateTavilyResults(
  params: EvaluateTavilyParams,
  tavilySearchResults: {
    query: string;
    results: { title: string; url: string; content: string }[];
  }[],
  log: Logger,
): Promise<TavilyEvaluationResponse>
</file>

<file path="src/app/(auth)/onboarding/_components/start-over-button.tsx">
import { useState, useTransition } from "react";
import { RotateCcw, Loader2 } from "lucide-react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
  AlertDialogTrigger,
} from "@/components/ui/alert-dialog";
import { resetOnboardingAction } from "@/app/(auth)/onboarding/actions";
import { useOnboardingStore } from "@/lib/store/onboarding-store";
⋮----
interface StartOverButtonProps {
  variant?: "default" | "destructive" | "outline" | "secondary" | "ghost" | "link";
  size?: "default" | "sm" | "lg" | "icon";
  className?: string;
}
⋮----
const handleReset = () =>
</file>

<file path="src/app/(auth)/onboarding/literature-review/_services/search-api.ts">
import type { RawPaper, ValidatedPaper } from "./literature-review-papers";
import type { FoundationalQuery } from "@/lib/types";
import type { Logger } from "@/lib/logger";
⋮----
function extractCleanDoi(raw: string | null | undefined): string | null
⋮----
function resolveAbstractInvertedIndex(
  invertedIndex: Record<string, number[]> | null | undefined,
): string | null
⋮----
function extractSignificantWords(query: string): string[]
⋮----
function parseOpenAlexResults(
  results: Record<string, unknown>[],
  query?: string,
): RawPaper[]
⋮----
// ============================================================================
// OpenAlex Query Executor (shared factory)
// ============================================================================
⋮----
async function queryOpenAlexWorks(
  params: URLSearchParams,
  query?: string,
): Promise<RawPaper[]>
⋮----
export async function searchOpenAlex(query: string): Promise<RawPaper[]>
⋮----
export async function searchOpenAlexKeyword(
  query: string,
): Promise<RawPaper[]>
⋮----
export async function validateWithCrossRef(
  paper: ValidatedPaper,
): Promise<ValidatedPaper>
⋮----
export interface FoundationalWorkResult {
  id: string;
  title: string;
  type: string;
  publicationYear: number;
  citedByCount: number;
  isFoundational: boolean;
  authors: string[];
  publisher: string | null;
}
⋮----
export async function resolveFoundationalWorks(
  queries: FoundationalQuery[],
  logger?: Logger,
): Promise<FoundationalWorkResult[]>
⋮----
export async function fetchFullAbstracts(
  openAlexIds: string[],
): Promise<Map<string, string>>
</file>

<file path="src/app/(auth)/onboarding/risk/_services/analysis.ts">
import { ThinkingLevel } from "@google/genai";
import { generateStructuredContent } from "@/lib/gemini";
import { extractMessage } from "@/lib/error-utils";
import type { Logger } from "@/lib/logger";
import type { AxesOption, TezaraThesisDetails } from "@/lib/types";
import {
  geminiAnalysisSchema,
  buildAnalysisSystemInstruction,
  buildAnalysisPrompt,
} from "@/lib/prompts";
⋮----
export interface AnalyzeOriginalityRiskParams {
  studyTitle: string;
  researchQuestion: string;
  mainClaim: string;
  theoreticalFramework: string;
  methodology: string;
  dataStrategy: string;
  historicalLimits: string;
  spatialLimits: string;
  analyticalFocus: string;
  validDetails: TezaraThesisDetails[];
}
⋮----
export interface CalculatedOverlapItem {
  id: number;
  title: string;
  author: string;
  university: string;
  year: number;
  thesisType: string;
  department: string;
  comparisonNote: string;
  axes: {
    subject: AxesOption;
    theory: AxesOption;
    methodology: AxesOption;
    context?: AxesOption;
  };
  originalityLevel: "HIGH_RISK" | "MEDIUM_RISK" | "LOW_RISK" | "ZERO_RISK";
}
⋮----
export interface CalculatedOriginalityRiskResult {
  originalityBadge: "HIGH_RISK" | "MEDIUM_RISK" | "LOW_RISK" | "ZERO_RISK";
  overlapTable: CalculatedOverlapItem[];
  riskPercentage: number;
}
⋮----
function badgeToRiskPercentage(
  badge: CalculatedOriginalityRiskResult["originalityBadge"],
): number
⋮----
export function calculateOriginalityRisk(
  overlapTable: Array<{
    id: number;
    academic_reasoning: string;
    originality_level: "HIGH_RISK" | "MEDIUM_RISK" | "LOW_RISK" | "ZERO_RISK";
    subject_overlap: "HIGH" | "PARTIAL" | "NONE";
    methodology_overlap: "HIGH" | "PARTIAL" | "NONE";
    theory_overlap: "HIGH" | "PARTIAL" | "NONE";
    context_overlap: "HIGH" | "PARTIAL" | "NONE";
  }>,
  validDetails: TezaraThesisDetails[],
): CalculatedOriginalityRiskResult
⋮----
export function getThesisPriority(axes: {
  subject: string;
  theory: string;
  methodology: string;
  context?: string;
}): number
⋮----
export async function analyzeOriginalityRisk(
  params: AnalyzeOriginalityRiskParams,
  log: Logger,
): Promise<
</file>

<file path="src/app/(auth)/onboarding/risk/_services/queries.ts">
import { ThinkingLevel } from "@google/genai";
import { generateStructuredContent } from "@/lib/gemini";
import { extractMessage } from "@/lib/error-utils";
import type { Logger } from "@/lib/logger";
import {
  factQueryExtractionSchema,
  buildFactQueryExtractionSystemInstruction,
  buildFactQueryPrompt,
  litKeywordExtractionSchema,
  buildLitKeywordExtractionSystemInstruction,
  buildLitKeywordPrompt,
} from "@/lib/prompts";
⋮----
interface FactQueryExtractionResponse {
  tavilyQueries: string[];
}
⋮----
interface LitKeywordExtractionResponse {
  keywords: string[];
}
⋮----
export interface ExtractQueriesParams {
  studyTitle: string;
  mainClaim: string;
  theoreticalFramework: string;
  methodology: string;
  dataStrategy: string;
  historicalLimits: string;
  spatialLimits: string;
  analyticalFocus: string;
}
⋮----
export async function extractQueries(
  params: ExtractQueriesParams,
  log: Logger,
): Promise<
⋮----
function getCombinations(arr: string[], size: number): string[]
⋮----
function helper(start: number, path: string[])
</file>

<file path="src/app/(auth)/onboarding/literature-review/_services/ai-processor.ts">
import { ThinkingLevel } from "@google/genai";
import { generateStructuredContent } from "@/lib/gemini";
import {
  buildLiteratureSiftingPrompt,
  buildLiteratureJuryAnalysisPrompt,
  buildLiteratureSiftingSystemInstruction,
  buildLiteratureJuryAnalysisSystemInstruction,
  literatureSiftingSchema,
  literatureJuryAnalysisSchema,
} from "@/lib/prompts";
import { Logger } from "@/lib/logger";
import type { SubBoxInput, ValidatedPaper } from "./literature-review-papers";
import type { JuryArticle } from "@/lib/types";
import { validateWithCrossRef } from "./search-api";
⋮----
export interface LiteratureReviewResult {
  starterPack: JuryArticle[];
  reservedPool: JuryArticle[];
  error?: string;
}
⋮----
interface SiftingResultItem {
  id: string;
  status: "ACCEPT" | "REJECT";
}
⋮----
interface SiftingResponse {
  siftedResults: SiftingResultItem[];
}
⋮----
export async function runSiftingStage(
  box: SubBoxInput,
  candidates: ValidatedPaper[],
  logger: Logger,
  thesisCtx: {
    studyTitle: string;
    researchQuestion: string;
    theoreticalFramework: string;
    historicalLimits: string;
    spatialLimits: string;
  },
): Promise<ValidatedPaper[]>
⋮----
interface JuryResponse {
  starterPack: JuryArticle[];
  reservedPool: JuryArticle[];
}
⋮----
export async function runJuryStage(
  box: SubBoxInput,
  sifted: ValidatedPaper[],
  logger: Logger,
): Promise<LiteratureReviewResult>
⋮----
// Build lookup for isFoundational backfill
⋮----
function backfillIsFoundational(
  articles: JuryArticle[],
  lookup: Map<string, boolean>,
): JuryArticle[]
⋮----
export async function enrichJuryArticleWithCrossRef(
  article: JuryArticle,
  pool: ValidatedPaper[],
): Promise<JuryArticle>
</file>

<file path="src/app/(auth)/onboarding/risk/_components/risk-container.tsx">
import { Loader2 } from "lucide-react";
import { Button } from "@/components/ui/button";
import { ErrorDisplay } from "@/components/error-display";
import { OriginalityReportView } from "./originality-report-view";
import { useRiskAnalysis } from "../_hooks/use-risk-analysis";
</file>

<file path="src/lib/tezara.ts">
import type { Logger } from "./logger";
import type { TezaraThesisSummary, TezaraThesisDetails } from "./types";
import {
  extractRscTexts,
  extractJsonObjects,
  parseRscTheses,
  findThesisObjRecursively,
} from "./tezara-parser";
⋮----
export async function searchTezaraPage(
  query: string,
  page: number,
  logger?: Logger,
  advanced = false,
): Promise<TezaraThesisSummary[]>
⋮----
export async function searchTezara(
  query: string,
  logger?: Logger,
  advanced = false,
): Promise<TezaraThesisSummary[]>
⋮----
export async function fetchThesisDetails(
  id: number,
  logger?: Logger,
): Promise<TezaraThesisDetails | null>
</file>

<file path="package.json">
{
  "name": "fabricca",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbo",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "check:full": "npm run lint && npm run typecheck"
  },
  "dependencies": {
    "@google/genai": "^2.8.0",
    "@neondatabase/serverless": "^1.0.0",
    "@radix-ui/react-accordion": "^1.2.14",
    "@radix-ui/react-alert-dialog": "^1.1.16",
    "@radix-ui/react-checkbox": "^1.3.4",
    "@radix-ui/react-label": "^2.1.9",
    "@radix-ui/react-scroll-area": "^1.2.11",
    "@radix-ui/react-slot": "^1.2.5",
    "@tanstack/react-query": "^5.101.0",
    "bcrypt-ts": "^6.0.0",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.43.1",
    "lucide-react": "^0.510.0",
    "next": "^15.3.1",
    "next-themes": "^0.4.6",
    "pg": "^8.15.6",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "sonner": "^2.0.7",
    "tailwind-merge": "^3.2.0",
    "tailwindcss-animate": "^1.0.7",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.6",
    "@types/node": "^22.15.3",
    "@types/pg": "^8.11.14",
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "drizzle-kit": "^0.30.6",
    "eslint": "^8.57.0",
    "eslint-config-next": "^15.3.1",
    "postcss": "^8.5.3",
    "repomix": "^1.15.0",
    "tailwindcss": "^4.1.6",
    "tsx": "^4.22.4",
    "typescript": "^5.8.3"
  }
}
</file>

<file path="src/app/(auth)/onboarding/literature-review/_components/literature-review-content.tsx">
import { Loader2, AlertCircle, BookOpen, Library } from "lucide-react";
import { Button } from "@/components/ui/button";
import { useOnboardingStore } from "@/lib/store/onboarding-store";
import type { GeminiThesisBox } from "@/lib/types";
import { LiteratureArticleCard } from "./literature-article-card";
import {
  useLiteratureReview,
  type BoxStatus,
} from "../_hooks/use-literature-review";
⋮----
function SubBoxQuery({
  status,
  errorMessage,
}: {
  status: BoxStatus;
  errorMessage?: string;
})
</file>

<file path="src/app/(auth)/onboarding/actions.ts">
import { eq } from "drizzle-orm";
import { db } from "@/db";
import { thesisMatrices, originalityReports, users } from "@/db/schema";
import { getSession } from "@/proxy";
import { revalidatePath } from "next/cache";
import { createFlowId, Logger } from "@/lib/logger";
⋮----
export async function resetOnboardingAction(): Promise<
  { success: boolean } | { error: string }
> {
  const flowId = createFlowId();
</file>

<file path="src/app/(auth)/onboarding/page.tsx">
import { redirect } from "next/navigation";
import { eq } from "drizzle-orm";
import { db } from "@/db";
import { thesisMatrices, originalityReports, thesisBoxes } from "@/db/schema";
import { getProfile } from "@/proxy";
⋮----
export default async function OnboardingPage()
</file>

<file path="src/lib/logger.ts">
import pino from "pino";
import pretty from "pino-pretty";
import fs from "fs";
import path from "path";
import { classifyError, getErrorDisplay } from "./error-utils";
⋮----
export type LogLevel = "info" | "warn" | "error";
export type LogEvent = "login_success" | "login_failed" | "flow_complete";
⋮----
export type ServiceName =
  | "gemini"
  | "cloudflare"
  | "tavily"
  | "tezara"
  | "db"
  | "auth"
  | "flow"
  | "matrix"
  | "enrichment"
  | "originality"
  | "complete"
  | "boxes"
  | "wikipedia"
  | "literature"
  | "library"
  | "openalex"
  | "crossref";
⋮----
export interface TokenUsage {
  input?: number;
  output?: number;
  total?: number;
}
⋮----
export interface PayloadData {
  flowId: string;
  timestamp: string;
  stage: string;
  module: string;
  prompt: string;
  response?: string;
}
⋮----
export interface LogParams {
  service?: ServiceName;
  step?: string;
  durationMs?: number;
  data?: Record<string, unknown>;
  error?: unknown;
  tokens?: TokenUsage;
  filePath?: string;
  status?: "START" | "SUCCESS" | "FAILED";
}
⋮----
function getTimestamp(): string
⋮----
function truncateFlowId(fid: string): string
⋮----
function getModuleDisplay(service: ServiceName, params?: LogParams): string
⋮----
function getActionDisplay(event: string):
⋮----
function getStatusTag(status: string): string
⋮----
function deriveStatus(event: string): string
⋮----
function truncate(s: string, maxLen: number): string
⋮----
/**
 * Central counter validator & normalizer.
 * Reads before/after and count-like fields from a data record,
 * validates internal consistency, and returns formatted metric strings.
 */
function processDataMetrics(data: Record<string, unknown>): string[]
⋮----
function peekStarted(flowId: string, event: string): boolean
⋮----
function markStarted(flowId: string, event: string): void
⋮----
export class Logger
⋮----
constructor(flowId: string)
⋮----
info(arg1: string | Record<string, unknown>, params?: LogParams): void
⋮----
warn(arg1: string | Record<string, unknown>, params?: LogParams): void
⋮----
error(arg1: string | Record<string, unknown>, params?: LogParams): void
⋮----
step(stepName: string, metadata?: Record<string, unknown>): void
⋮----
/**
   * Log a file reference (path or URL) for traceability.
   */
file(ref: string): void
⋮----
data(label: string, value: unknown): void
⋮----
preview(label: string, value: unknown): void
⋮----
prompt(model: string, content: string): void
⋮----
saveDebugPayload(
    stage: string,
    module: string,
    prompt: string,
    response?: string,
): string | undefined
⋮----
private write(
    level: "info" | "warn" | "error",
    arg1: string | Record<string, unknown>,
    params?: LogParams,
): void
⋮----
private renderEventLine(
    service: ServiceName,
    event: string,
    status: string,
    params?: LogParams,
): void
⋮----
private buildPrefix(service: ServiceName | string, event: string): string
⋮----
private buildSuffix(
    event: string,
    status: string,
    params?: LogParams,
): string
⋮----
private renderDevLine(data: Record<string, unknown>): void
⋮----
export function createFlowId(): string
</file>

<file path="src/app/(auth)/onboarding/boxes/actions.ts">
import { eq } from "drizzle-orm";
import { db } from "@/db";
import { thesisBoxes } from "@/db/schema";
import { getSession } from "@/proxy";
import { generateStructuredContent } from "@/lib/gemini";
import { ThinkingLevel } from "@google/genai";
import { createFlowId, Logger } from "@/lib/logger";
import { revalidatePath } from "next/cache";
import {
  buildThesisBoxGenerationSystemInstruction,
  buildThesisBoxGenerationPrompt,
  thesisBoxGenerationSchema,
} from "@/lib/prompts";
import type { GeminiThesisBox, OnboardingActionResult } from "@/lib/types";
import { fetchThesisMatrix } from "../_lib/fetch-actions";
⋮----
export async function generateBoxesAction(): Promise<
  { success: true; boxes: GeminiThesisBox[] } | { error: string }
> {
  const flowId = createFlowId();
⋮----
export async function confirmBoxesAction(
  boxes: GeminiThesisBox[],
): Promise<OnboardingActionResult>
</file>

<file path="src/app/(auth)/onboarding/matrix/_components/matrix-form.tsx">
import { useState, useEffect } from "react";
import { useRouter } from "next/navigation";
import { toast } from "sonner";
import { Loader2 } from "lucide-react";
⋮----
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { useOnboardingStore } from "@/lib/store/onboarding-store";
import type { LoadingStep } from "@/lib/store/onboarding-store";
import { enrichThesisMatrixAction, saveEnrichedMatrixAction } from "../actions";
import { fetchThesisMatrix } from "../../_lib/fetch-actions";
⋮----
const handleSubmit = async (e: React.FormEvent<HTMLFormElement>) =>
</file>

<file path="src/app/(auth)/onboarding/matrix/actions.ts">
import { eq } from "drizzle-orm";
import { db } from "@/db";
import { thesisMatrices, originalityReports, thesisBoxes } from "@/db/schema";
import { getSession } from "@/proxy";
import { generateStructuredContent } from "@/lib/gemini";
import { ThinkingLevel } from "@google/genai";
import { createFlowId, Logger } from "@/lib/logger";
⋮----
import {
  enhancedThesisSchema,
  buildMatrixEnhancementSystemInstruction,
  buildMatrixEnhancementPrompt,
} from "@/lib/prompts";
import type { EnhancedThesisData } from "@/lib/types";
⋮----
export type ThesisMatrixInput = {
  studyTitle: string;
  researchQuestion: string;
  mainClaim: string;
  theoreticalFramework: string;
  methodology: string;
  dataStrategy: string;
  historicalLimits: string;
  spatialLimits: string;
  analyticalFocus: string;
};
⋮----
function validateField(value: string | undefined): string | null
⋮----
/**
 * Validates the raw thesis matrix input, sends it to Gemini for academic
 * enrichment, and returns the enhanced result WITHOUT writing to the database.
 *
 * @param data - Raw thesis matrix fields from the onboarding form
 * @returns Enhanced thesis data on success, or a validation/Gemini error message
 */
export async function enrichThesisMatrixAction(
  data: ThesisMatrixInput,
): Promise<
⋮----
export async function saveEnrichedMatrixAction(
  enhancedData: EnhancedThesisData,
): Promise<
</file>

<file path="src/app/(auth)/onboarding/risk/_services/sifting.ts">
import { ThinkingLevel } from "@google/genai";
import { generateStructuredContent } from "@/lib/gemini";
import { extractMessage } from "@/lib/error-utils";
import { cosineSimilarity } from "@/lib/utils";
import { generateEmbeddings } from "@/lib/cloudflare";
import { fetchThesisDetails } from "@/lib/tezara";
import type { Logger } from "@/lib/logger";
import type {
  TezaraThesisSummary,
  TezaraThesisDetails,
  DeepSiftEntry,
  DeepSiftResponse,
} from "@/lib/types";
import {
  deepSiftingSchema,
  buildDeepSiftingSystemInstruction,
  buildDeepSiftingPrompt,
} from "@/lib/prompts";
⋮----
export interface SiftAndFetchDetailsParams {
  studyTitle: string;
  researchQuestion: string;
  mainClaim: string;
  theoreticalFramework: string;
  methodology: string;
  dataStrategy: string;
  historicalLimits: string;
  spatialLimits: string;
  analyticalFocus: string;
}
⋮----
export interface SiftingDiagnostic {
  uniqueAfterDedup: number;
  topSimilarities: { id: number; score: number }[];
  stage1Count: number;
  fetchRequested: number;
  fetchSuccess: number;
  fetchFailed: number;
}
⋮----
export async function siftAndFetchDetails(
  params: SiftAndFetchDetailsParams,
  tezaraSearchResults: TezaraThesisSummary[][],
  log: Logger,
): Promise<
</file>

<file path="src/app/(auth)/onboarding/boxes/_components/boxes-container.tsx">
import { useEffect, useState, useCallback } from "react";
import { useRouter } from "next/navigation";
import { toast } from "sonner";
import {
  Loader2,
  CheckCircle2,
  Rocket,
  Library,
  FileText,
  PlusCircle,
  WholeWord,
} from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardTitle } from "@/components/ui/card";
import { confirmBoxesAction } from "../actions";
import { useOnboardingStore } from "@/lib/store/onboarding-store";
import type { GeminiThesisBox } from "@/lib/types";
⋮----
// No boxes anywhere — redirect to risk page to generate them.
</file>

<file path="src/app/(auth)/onboarding/literature-review/actions.ts">
import { eq } from "drizzle-orm";
import { cookies } from "next/headers";
import { revalidatePath } from "next/cache";
import { db } from "@/db";
import {
  thesisMatrices,
  thesisBoxes,
  libraryResources,
  users,
} from "@/db/schema";
import { getSession } from "@/proxy";
import { Logger, createFlowId } from "@/lib/logger";
import type {
  LiteraturePoolEntry,
  OnboardingActionResult,
  JuryArticle,
} from "@/lib/types";
import type { NewLibraryResource } from "@/db/schema";
import type {
  SubBoxInput,
  RawPaper,
  ValidatedPaper,
} from "./_services/literature-review-papers";
import { mergePapers } from "./_services/literature-review-papers";
import {
  searchOpenAlex,
  searchOpenAlexKeyword,
  resolveFoundationalWorks,
  fetchFullAbstracts,
} from "./_services/search-api";
import {
  runSiftingStage,
  runJuryStage,
  enrichJuryArticleWithCrossRef,
  type LiteratureReviewResult,
} from "./_services/ai-processor";
⋮----
async function enrichBatch(
  articles: JuryArticle[],
  pool: ValidatedPaper[],
): Promise<JuryArticle[]>
⋮----
async function processSingleBox(
  subBox: SubBoxInput,
  thesisCtx: {
    studyTitle: string;
    researchQuestion: string;
    theoreticalFramework: string;
    historicalLimits: string;
    spatialLimits: string;
  },
  logger: Logger,
): Promise<LiteratureReviewResult>
⋮----
// ------------------------------------------------------------------
// Stage 6: CrossRef polite-pool validation (concurrency-limited)
// ------------------------------------------------------------------
⋮----
export async function processLiteratureReviewAction(
  subBoxes: SubBoxInput[],
): Promise<
⋮----
export async function confirmLiteratureAction(args: {
  literaturePool: LiteraturePoolEntry[];
}): Promise<OnboardingActionResult>
</file>

<file path="src/app/(auth)/onboarding/risk/_components/originality-report-view.tsx">
import { useMemo } from "react";
import { Layers } from "lucide-react";
import { StartOverButton } from "../../_components/start-over-button";
import { TavilyFactCheckTable } from "./tavily-fact-check-table";
import { TezaraOverlapTable } from "./tezara-overlap-table";
import { StrategicRoadmapSection } from "./strategic-roadmap-section";
import { statusTranslation, BADGE_COLORS } from "../_lib/constants";
import type { OriginalityReportData } from "@/lib/types";
⋮----
interface OriginalityReportViewProps {
  reportData: OriginalityReportData;
}
⋮----
interface RoleDistribution {
  label: string;
  key: "HIGH_RISK" | "MEDIUM_RISK" | "LOW_RISK" | "ZERO_RISK";
  count: number;
}
⋮----
export function OriginalityReportView(
</file>

<file path="src/lib/types.ts">
export type OnboardingActionResult =
  | { success: true; isProcessing?: boolean; error?: never }
  | { success?: never; error: string };
⋮----
export type EnhancedThesisData = {
  academicStudyTitle: string;
  literatureResearchQuestion: string;
  refinedThesisClaim: string;
  conceptualTheoreticalInfrastructure: string;
  academicMethodologyDesign: string;
  dataStrategy: string;
  historicalLimits: string;
  spatialLimits: string;
  analyticalFocus: string;
};
⋮----
export type EnhancedThesisActionResult =
  | { success: true; data: EnhancedThesisData; error?: never }
  | { success?: never; error: string };
⋮----
export type AxesOption = "HIGH" | "PARTIAL" | "NONE";
⋮----
export interface TezaraThesisSummary {
  id: number;
  title: string;
  author: string;
  university: string;
  year: number;
  thesisType: string;
  department: string;
}
⋮----
export interface TezaraThesisDetails {
  id: number;
  title: string;
  author: string;
  university: string;
  year: number;
  thesisType: string;
  department: string;
  abstract: string;
  yokPdfUrl?: string;
}
⋮----
export interface QueryExtractionResponse {
  tavilyQueries: string[];
  keywords: string[];
}
⋮----
export interface DeepSiftEntry {
  id: number;
  positioning: "HIGH" | "PARTIAL" | "NONE";
}
⋮----
export interface DeepSiftResponse {
  selectedTheses: DeepSiftEntry[];
}
⋮----
export interface TavilyEvaluationFact {
  fact: string;
  result: "VERIFIED" | "PARTIALLY_VERIFIED" | "REFUTED";
  resultNote?: string;
  sourceUrl: string;
}
⋮----
export interface TavilyEvaluationResponse {
  items: TavilyEvaluationFact[];
  briefingNote: string;
}
⋮----
export interface OverlapItem {
  id: number;
  axes: {
    subject: AxesOption;
    theory: AxesOption;
    methodology: AxesOption;
    context?: AxesOption;
  };
  comparisonNote?: string;
  yokPdfUrl?: string;
}
⋮----
export interface OriginalityReportData {
  originalityScore?: number;
  originalityBadge?: "HIGH_RISK" | "MEDIUM_RISK" | "LOW_RISK" | "ZERO_RISK";
  overlapAnalysis?: OverlapItem[];
  synthesisRoadmap?: string;
  tavilyResults: {
    items: TavilyEvaluationFact[];
    briefingNote: string;
  };
  tezaraResults: {
    originalityBadge: "HIGH_RISK" | "MEDIUM_RISK" | "LOW_RISK" | "ZERO_RISK";
    overlapTable: (OverlapItem & {
      title: string;
      author: string;
      university: string;
      year: number;
      thesisType: string;
      department: string;
      originalityLevel: "HIGH_RISK" | "MEDIUM_RISK" | "LOW_RISK" | "ZERO_RISK";
    })[];
    strategicRecommendations: string;
    riskPercentage?: number;
  };
}
⋮----
export type StepStatus = "idle" | "loading" | "error" | "success";
⋮----
export type OnboardingStep =
  | "matrix"
  | "enrichment"
  | "risk"
  | "boxes"
  | "literature-review"
  | "completed";
⋮----
export interface OnboardingFormData {
  studyTitle: string;
  researchQuestion: string;
  mainClaim: string;
  theoreticalFramework: string;
  methodology: string;
  dataStrategy: string;
  historicalLimits: string;
  spatialLimits: string;
  analyticalFocus: string;
}
⋮----
export interface ScrapedTheses {
  selected: TezaraThesisDetails[];
  eliminated: TezaraThesisSummary[];
}
⋮----
export interface JuryReportItem {
  id: number;
  title: string;
  author: string;
  university: string;
  year: number;
  thesisType: string;
  department: string;
  originalityLevel: "HIGH_RISK" | "MEDIUM_RISK" | "LOW_RISK" | "ZERO_RISK";
  axes: {
    subject: AxesOption;
    theory: AxesOption;
    methodology: AxesOption;
    context?: AxesOption;
  };
  comparisonNote?: string;
  yokPdfUrl?: string;
}
⋮----
export interface FoundationalQuery {
  author: string;
  title: string;
  publicationYear: number;
}
⋮----
export interface GeminiThesisBox {
  title: string;
  description: string;
  semanticSearchBlock: string;
  foundationalQueries: FoundationalQuery[];
  concepts: string[];
}
⋮----
export interface JuryArticle {
  type: "PRIMARY" | "SECONDARY";
  title: string;
  abstract: string;
  url: string;
  doi: string;
  publisher: string;
  publicationYear: number;
  authors: string[];
  isFoundational?: boolean;
}
⋮----
export interface LiteraturePoolEntry {
  subBoxTitle: string;
  starterPack: JuryArticle[];
  reservedPool: JuryArticle[];
}
</file>

<file path="src/app/(auth)/onboarding/enrichment/_components/enrichment-view.tsx">
import { useState, useEffect } from "react";
import { useRouter } from "next/navigation";
import { toast } from "sonner";
import { Loader2 } from "lucide-react";
⋮----
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { confirmEnhancedThesisAction } from "../actions";
import {
  extractQueriesAction,
  executeSearchAction,
  siftThesesAction,
  finalizeJuryAnalysisAction,
} from "../../risk/actions";
import { fetchThesisMatrix } from "../../_lib/fetch-actions";
import { useOnboardingStore } from "@/lib/store/onboarding-store";
import type { LoadingStep } from "@/lib/store/onboarding-store";
import type { EnhancedThesisData } from "@/lib/types";
⋮----
const handleConfirm = async (e: React.FormEvent<HTMLFormElement>) =>
</file>

<file path="src/lib/gemini.ts">
import { GoogleGenAI, ThinkingLevel } from "@google/genai";
import type { Logger } from "./logger";
⋮----
export interface JsonSchemaProperty {
  type: string;
  items?:
    | JsonSchemaProperty
    | {
        type: string;
        enum?: string[];
        properties?: Record<string, JsonSchemaProperty>;
        required?: string[];
      };
  properties?: Record<string, JsonSchemaProperty>;
  required?: string[];
  enum?: string[];
  description?: string;
  minLength?: number;
  maxLength?: number;
  minItems?: number;
  maxItems?: number;
}
⋮----
export interface JsonSchema {
  type: "object";
  properties: Record<string, JsonSchemaProperty>;
  required?: string[];
}
⋮----
function getAi(): GoogleGenAI
⋮----
async function retryOn503<T>(
  fn: () => Promise<T>,
  maxRetries = 3,
  baseDelayMs = 1000,
  logger?: Logger,
): Promise<
⋮----
export async function generateStructuredContent<T>(
  modelName: string,
  systemInstruction: string,
  prompt: string,
  schema: JsonSchema,
  logger?: Logger,
  options?: {
    thinkingConfig?: {
      thinkingLevel?: ThinkingLevel;
    } | null;
    payloadStage?: string;
  },
): Promise<T>
⋮----
// 2. Attempt parsing the cleaned text
⋮----
// Save debug payload
</file>

<file path="src/app/(auth)/onboarding/risk/actions.ts">
import { eq } from "drizzle-orm";
import { db } from "@/db";
import { originalityReports, thesisMatrices, thesisBoxes } from "@/db/schema";
import { getSession } from "@/proxy";
import { createFlowId, Logger } from "@/lib/logger";
import { revalidatePath } from "next/cache";
import type {
  OnboardingActionResult,
  ScrapedTheses,
  TavilyEvaluationResponse,
  TezaraThesisSummary,
  OriginalityReportData,
} from "@/lib/types";
⋮----
import { extractQueries } from "./_services/queries";
import {
  executeParallelSearch,
  evaluateTavilyResults,
} from "./_services/search";
import { siftAndFetchDetails } from "./_services/sifting";
import {
  analyzeOriginalityRisk,
  calculateOriginalityRisk,
} from "./_services/analysis";
import { synthesizeRoadmap } from "./_services/roadmap";
⋮----
interface OnboardingMatrixInput {
  studyTitle: string;
  researchQuestion: string;
  mainClaim: string;
  theoreticalFramework: string;
  methodology: string;
  dataStrategy: string;
  historicalLimits: string;
  spatialLimits: string;
  analyticalFocus: string;
}
⋮----
export async function extractQueriesAction(
  matrix: OnboardingMatrixInput,
): Promise<
  | {
      success: true;
      data: {
        tavilyQueries: string[];
        tezaraQueries: string[];
        keywords: string[];
      };
    }
  | { error: string }
> {
  const flowId = createFlowId();
⋮----
export async function executeSearchAction(params: {
  studyTitle: string;
  tavilyQueries: string[];
  tezaraQueries: string[];
}): Promise<
  | {
      success: true;
      data: {
        tezaraSearchResults: TezaraThesisSummary[][];
        tavilyResults: TavilyEvaluationResponse;
      };
    }
  | { error: string }
> {
  const flowId = createFlowId();
⋮----
export async function siftThesesAction(params: {
  matrix: OnboardingMatrixInput;
  tezaraSearchResults: TezaraThesisSummary[][];
}): Promise<
⋮----
export async function finalizeJuryAnalysisAction(params: {
  matrix: OnboardingMatrixInput;
  scrapedTheses: ScrapedTheses;
  tavilyResults: TavilyEvaluationResponse;
}): Promise<
  { success: true; data: OriginalityReportData } | { error: string }
> {
  const flowId = createFlowId();
⋮----
// Single database write at the very end of the pipeline
⋮----
export async function completeRiskStageAction(): Promise<OnboardingActionResult>
</file>

<file path="src/db/schema.ts">
import {
  pgTable,
  serial,
  varchar,
  text,
  integer,
  timestamp,
  jsonb,
  pgEnum,
  boolean,
  index,
} from "drizzle-orm/pg-core";
import type { InferSelectModel, InferInsertModel } from "drizzle-orm";
⋮----
export type User = InferSelectModel<typeof users>;
⋮----
export type NewUser = InferInsertModel<typeof users>;
⋮----
export type ThesisMatrix = InferSelectModel<typeof thesisMatrices>;
⋮----
export type NewThesisMatrix = InferInsertModel<typeof thesisMatrices>;
⋮----
export type OriginalityReport = InferSelectModel<typeof originalityReports>;
⋮----
export type NewOriginalityReport = InferInsertModel<typeof originalityReports>;
⋮----
export type ThesisBox = InferSelectModel<typeof thesisBoxes>;
⋮----
export type NewThesisBox = InferInsertModel<typeof thesisBoxes>;
⋮----
export type LibraryResource = InferSelectModel<typeof libraryResources>;
⋮----
export type NewLibraryResource = InferInsertModel<typeof libraryResources>;
</file>

</files>



<instruction>
# Proje Analiz Talimatı

Bu dosya, projenin kaynak kodlarını içermektedir. Lütfen kod yapısını ve mimariyi analiz et.

</instruction>
