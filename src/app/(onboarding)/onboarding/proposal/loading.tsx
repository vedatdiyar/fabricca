import { OnboardingStepSkeleton } from "../_components/onboarding-step-skeleton";

/**
 * Skeleton loading UI for the onboarding proposal step.
 *
 * @returns The onboarding step page skeleton.
 */
export default function ProposalLoading() {
  return <OnboardingStepSkeleton />;
}
