import { OnboardingStepSkeleton } from "./_components/onboarding-step-skeleton";

/**
 * Generic fallback loading skeleton for the onboarding route hierarchy.
 *
 * @returns The onboarding step skeleton markup.
 */
export default function OnboardingLoading() {
  return <OnboardingStepSkeleton />;
}
