/** Maximum file size for PDF uploads in bytes (50MB). */
export const PDF_MAX_FILE_SIZE = 50 * 1024 * 1024;

/** Allowed PDF file extensions. */
export const ALLOWED_PDF_EXTENSIONS = [".pdf"] as const;

/** MIME type for PDF files. */
export const PDF_MIME_TYPE = "application/pdf";
